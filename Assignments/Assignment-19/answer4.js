const num1 = 10;
const num2 = 25;

for (let i = num1 + 1; i <= num2; i++) {
//   process.stdout.write(i + " ");
console.log(i);
}


//process.stdout.write is a method used in Node.js to output data to the standard output (usually the console) without adding a newline character at the end. 